# kubectx_rs

A simple Rust clone of kubectx

Learning rust by rewriting a tool I use every day. I suggest not using this.

Currently supports:

* Listing contexts
* Setting contexts
* Deleting contexts
